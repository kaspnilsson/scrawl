/**
 * Displays an interactive multiple choice component.
 */

import { ProjectUpdateAttrs } from './ProjectUpdateAttrs'

const ProjectUpdateComponent = ({ projectName }: ProjectUpdateAttrs) => {
  return <div></div>
}

export default ProjectUpdateComponent
